#include <iostream>


using namespace std;

int main() {
  
  int second;
  double sec;
  String str;
  cin >> second;
  cin >> sec;
  cin >> str;
  cout << i + second << endl;   // i is already defined by the compiler
  cout << d + sec << endl;      // d is already defined by the compiler
  cout << s + str << endl;      // s is already defined by the compiler

   return 0;
   }